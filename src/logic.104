// Bottom part of the beanstalk
if (v80 == v0) {
  if (said("climb","beanstalk")) {
    if (isset(f157)) {
      print("You are already climbing the"
            "beanstalk.");
    }
    if (isset(f79)) {
      print("You can't do that when you are"
            "invisible!");
    }
  }
  if (said("check","beanstalk")) {
    print("It is a mighty beanstalk that stretches"
          "to the sky.");
  }
}
return();

// Messages
#message 1 "You are already climbing the beanstalk."
#message 2 "You can't do that when you are invisible!"
#message 3 "It is a mighty beanstalk that stretches to the sky."

